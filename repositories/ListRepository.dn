const char debugMSG[] = "[@ListRepository]"

component provides repositories.ListRepository {
    void ListRepository:append(store int num) {
        list = new int[](list, num)
        sum += num
    }

    int[] ListRepository:getList() {
        return list
    }

    int ListRepository:getSum() {
        return sum
    }
}
