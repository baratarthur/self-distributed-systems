uses time.Calendar
uses time.DateUtil

interface LatencyStore {
    int getMilisecondsExecutionTimeSince(DateTime start)
    dec getCurrentAverageLatency()
    void storeExecutionTime(int miliseconds)
}
